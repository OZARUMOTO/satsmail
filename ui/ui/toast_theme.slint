// SPDX-FileCopyrightText: 2026 Foundation Devices, Inc. <hello@foundation.xyz>
// SPDX-License-Identifier: MIT OR Apache-2.0

// GENERATED from toast.schema.json by foundation theme tooling — do not edit by hand.
// Token-backed props reference the shared Theme global; JSON literals are inlined.

import { Theme, ControlSize } from "theme.slint";

export enum ToastState {
    normal,
}

export struct ToastVariantProps {
    card-border-width: length,
    action-font-weight: int,
    message-color: color,
    icon-color: color,
    action-color: color,
    close-icon-color: color,
    card-border-color: color,
    toast-background: color,
}

export struct ToastSizeProps {
    font-family: string,
    font-size: length,
    card-width: length,
    toast-padding: length,
    icon-size: length,
    border-radius: length,
    action-font-size: length,
    close-button-size: length,
    action-height: length,
    action-horizontal-padding: length,
    action-radius: length,
    close-icon-size: length,
}

export global ToastTheme {
    public pure function style(st: ToastState) -> ToastVariantProps {
        return {
            card-border-width: 1px,
            action-font-weight: Theme.font-weight-bold,
            message-color: Theme.color-foreground,
            icon-color: Theme.color-primary,
            action-color: Theme.color-primary,
            close-icon-color: Theme.color-muted,
            card-border-color: Theme.color-border,
            toast-background: Theme.color-surface,
        };
    }
    public pure function size(s: ControlSize) -> ToastSizeProps {
        if s == ControlSize.sm {
            return {
                font-family: Theme.font-primary,
                font-size: Theme.font-size-sm,
                card-width: 300px,
                toast-padding: 12px,
                icon-size: 20px,
                border-radius: Theme.radius-sm,
                action-font-size: 14px,
                close-button-size: 28px,
                action-height: 26px,
                action-horizontal-padding: 10px,
                action-radius: 13px,
                close-icon-size: 18px,
            };
        }
        if s == ControlSize.md {
            return {
                font-family: Theme.font-primary,
                font-size: Theme.font-size-md,
                card-width: 360px,
                toast-padding: 14px,
                icon-size: 24px,
                border-radius: Theme.radius-sm,
                action-font-size: 14px,
                close-button-size: 32px,
                action-height: 26px,
                action-horizontal-padding: 10px,
                action-radius: 13px,
                close-icon-size: 20px,
            };
        }
        return {
            font-family: Theme.font-primary,
            font-size: Theme.font-size-lg,
            card-width: 420px,
            toast-padding: 16px,
            icon-size: 28px,
            border-radius: Theme.radius-sm,
            action-font-size: 14px,
            close-button-size: 36px,
            action-height: 26px,
            action-horizontal-padding: 10px,
            action-radius: 13px,
            close-icon-size: 22px,
        };
    }
}
